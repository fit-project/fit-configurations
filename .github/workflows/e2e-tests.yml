# SPDX-License-Identifier: LGPL-3.0-or-later
# Copyright (c) 2023 FIT-Project

name: E2E tests

on:
  push:
    branches:
      - main
    paths:
      - "fit_configurations/**"
      - "tests/e2e/**"
      - "tests/conftest.py"
      - "pyproject.toml"
      - "poetry.lock"
      - ".github/actions/setup-fit-env/**"
      - ".github/workflows/e2e-tests.yml"
  pull_request:
    branches:
      - main
    paths:
      - "fit_configurations/**"
      - "tests/e2e/**"
      - "tests/conftest.py"
      - "pyproject.toml"
      - "poetry.lock"
      - ".github/actions/setup-fit-env/**"
      - ".github/workflows/e2e-tests.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  e2e-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      QT_QPA_PLATFORM: minimal
      PYTHONDONTWRITEBYTECODE: "1"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup environment
        uses: ./.github/actions/setup-fit-env
        with:
          python-version: "3.11"
          system-packages: "libcairo2-dev pkg-config libegl1 libgl1 libpulse0"
          python-packages: "pytest"

      - name: Run e2e tests
        run: |
          python -m pytest -m e2e -q tests/e2e
